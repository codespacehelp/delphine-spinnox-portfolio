---
layout: base
---
<h1>PRODUCTPAGINA </h1>

<p>{{ description }}</p>
<h2>Dit kost {{ price }} euro!!!!</h2>

{{ content }}

<div class="product-images">
    {% for image in images %}
        <img src="/static/products/{{ image }}" alt="product image">
    {% endfor %}
</div>